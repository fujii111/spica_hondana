<h1>会員一覧</h1><% @title = "会員一覧" %>

<p>ログインIDをクリックするとその会員としてログインします。</p>

<table class="list">
  <thead>
    <tr>
      <th>ログインID</th>
      <th>ニックネーム</th>
      <th>氏名</th>
      <th>住所</th>
      <th>メールアドレス</th>
      <th>ブク</th>
    </tr>
  </thead>

  <tbody>
    <% @members.each do |member| %>
      <tr>
        <td><%= link_to member.login_id, "/members/login_as/" + member.id.to_s %></td>
        <td><%= member.nickname %></td>
        <td><%= member.name %></td>
        <td><%= member.address %></td>
        <td><%= member.mail_address %></td>
        <td class="right"><%= member.point %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p><%= link_to '新規会員登録', "/members/new" %></p>
